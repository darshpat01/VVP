if (annyang) {
    // Let's define a command.
    const commands = {
      'play': () => {
      console.log('play');
        player.play();
      },'pause': () => {
        console.log('pause');
          player.pause();
        },'stop': () => {
            console.log('stop');
              player.pause();
            }
    };
  
    // Add our commands to annyang
    annyang.addCommands(commands);
  
    // Start listening.
    annyang.start();
  }
